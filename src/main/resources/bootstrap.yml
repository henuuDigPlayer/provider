spring:
  application:
    name: provider-lindj
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      name: provider-lindj
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.1.46:8000/eureka/
